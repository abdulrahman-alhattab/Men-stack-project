<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>show page</title>
  <link rel="stylesheet" href="/css/details.css">
</head>

<body>
  <%- include('../partials/nav.ejs') %>
  <div id="car-detail">
    <h1><%= car.year %> <%= car.name %></h1>
    <h2>Made in:<%= car.country %></h2>
    <img src="<%= car.image %>" alt="">
    <h2>costs:<%= car.price %></h2>
    <h2>Damaged?:<%= car.damaged %></h2>
    <h3><%= car.description %></h3>

    <% if (car.owner._id.equals(user._id)) { %>
    <form action="/cars/<%= car._id %>?_method=DELETE" method="POST">

      <a id="edit-btn" href="/cars/<%= car._id %>/edit">Edit car</a>

      <button id="delete-btn" type="submit">Delete this car</button>
    </form>
    <% } %>
  </div>
</body>

</html>
